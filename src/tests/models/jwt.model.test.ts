describe('JWTModel', () => {
  test.todo('restrict to create record if user exists in collection');

  test.todo('generates token for user on presave');

  test.todo('has method for detect expired token');
});
